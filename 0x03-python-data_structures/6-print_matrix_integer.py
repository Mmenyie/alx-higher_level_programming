#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    if len(matrix) == 0:
        return
    if len(matrix) == 0:
        for integer in row:
            print(str.format("{0}", integer), end=" ")
        print()
